<?xml version="1.0" encoding="UTF-8"?>
<builder name="UserMapper">

    <mapper name="findUserByName">

        select * from user where` 1 = 1

        <choose>
            <if test="$req != null">
                <cond>and username1 = {{username1}}</cond>
                <cond>and username2 = {{username2}}</cond>
            </if>
            <else>
                <cond>and username3 = {{username3}}</cond>
                <cond>and username4 = {{username4}}</cond>
            </else>
        </choose>

    </mapper>

</builder>